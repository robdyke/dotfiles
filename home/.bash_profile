# by default, the version of bash (3.2.something) with os X does not source bashrc on login
#[ "`uname`" == 'Darwin' ] && source ~/.bashrc

# ...and Ubuntu won't load .bashrc if there is a .bash_profile so...
source ~/.bashrc
